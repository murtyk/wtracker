<%= render klass_titles %>
<% if klass = klass_titles.first && klass_titles.first.klass %>
  <% if (kt_ids = klass.klass_titles_ids_to_refresh_counts).any? %>
    <script type="text/javascript">
      refresh_job_counts(<%= raw kt_ids.to_json %>);
    </script>
  <% end %>
<% end %>