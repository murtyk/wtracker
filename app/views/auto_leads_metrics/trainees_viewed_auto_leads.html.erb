<h2>Following trainees viewed at least one job lead</h2>
<div><%= will_paginate(@data.trainees) %></div>

<ol>
  <%= @data.trainees.map{ |t| '<li>' + name_and_link(t) + '</li>' }.join('').html_safe %>
</ol>
