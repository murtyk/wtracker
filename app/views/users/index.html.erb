<div class = 'row-fluid page-caption-div' >
	<h1>Users</h1>
</div>

<div class = 'row-fluid'>
  <%= new_button(User) %>
</div>

<table class="table table-bordered table-striped table-hover">
<thead>
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th>Location</th>
    <th>Role</th>
    <th>Status</th>
    <% if current_grant.trainee_applications? %>
      <th>Counties</th>
    <% end %>
    <th>Online?</th>
    <th>Actions</th>
  </tr>
</thead>

<tbody>
  <% @users.each do |user| %>
    <tr>
      <td>  <%= user.name %>     </td>
      <td>  <%= user.email %>    </td>
      <td>  <%= user.location %> </td>
      <td>  <%= user.role %>     </td>
      <td>  <%= user.status %>   </td>
      <% if current_grant.trainee_applications? %>
        <td><%= user.assigned_county_names %></td>
      <% end %>
      <td>  <%= user.online_status %> </td>
      <td>
        <%= show_button(user) %>
        <%= edit_button(user) %>
      </td>
    </tr>
  <% end %>
</tbody>
</table>
