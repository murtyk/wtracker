<%= simple_form_for(assessment) do |f| %>
  <%= f.error_notification %>
  <div class="form-inputs">
    <%= f.input :name %>
  </div>
  <div class="form-actions">
    <%= cancel_button(f) %>
    <%= submit_button(f) %>
  </div>
<% end %>
