# derived placement status is stored in Trainee
# update all with current status

Account.all.each do |a|
  Account.current_id = a.id
  Grant.all.each do |g|
    Grant.current_id = g.id
    TraineeInteraction.where(termination_date: nil).each do |ti|
      ti.update_trainee_status
    end
    Trainee.where(status: nil).update_all(status: 0)
  end
end


Pending:

1. delete TI unused attributes
2. delete Grant Trainee Status MVC and routes