1. delete TIs with status != 4

counts = []

Account.all.each do |a|
  Account.current_id = a.id
  Grant.all.each do |g|
    Grant.current_id = g.id
    tis = TraineeInteraction.where.not(status: 4)
    counts << [a.id, g.id, tis.count]
  end
end

verify:
---------

counts


Account.all.each do |a|
  Account.current_id = a.id
  Grant.all.each do |g|
    Grant.current_id = g.id
    tis = TraineeInteraction.where.not(status: 4)
    tis.destroy_all
  end
end

re-run counts query again

and

Account.all.each do |a|
  Account.current_id = a.id
  Grant.all.each do |g|
    Grant.current_id = g.id
    counts << [a.id, g.id, TraineeInteraction.count]
  end
end


2. Deploy
3. db migrate
